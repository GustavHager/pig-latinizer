addons:
    apt:
        packages:
            - cmake
            - cmake-data
        sources:
            - george-edison55-precise-backports

language: c++

before_script:
    - mkdir build
    - cd build
    - cmake ..

script:
    - make

compiler:
    - clang
    - gcc
