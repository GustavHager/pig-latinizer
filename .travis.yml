addon:
    apt:
        packages:
            - cmake
            - cmake-data
        sources:
            - george-edison55-precise-backports

language: c++

before_script:
    - mkdir build
    - cd build
    - cmake ..

compiler:
    - clang
    - gcc
